import styled from '@emotion/styled'
import { color, space, flexbox, layout, background, border, position, shadow, typography, grid  } from 'styled-system'

export const Box = styled("div")(color, space, layout, flexbox, background, border, position, shadow, typography, grid );
